<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Pic2Cook.MainPage"
             BackgroundColor="Beige">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="logo.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="Pic 2 Cook"/>

            <Button
                x:Name="ScanFridgeBtn"
                Text="Scan Fridge" 
                SemanticProperties.Hint="Opens the camera to scan the fridge"
                Clicked="OnScanFridgeClicked"
                HorizontalOptions="Fill"
                BackgroundColor="LightCoral"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
